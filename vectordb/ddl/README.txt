# This directory contains SQL scripts that run automatically during database initialization
# Files are executed in alphabetical order

# Initialization scripts (auto-run on first startup):
# - 01_create_extension.sql: Enables the pgvector extension
# - 02_create_tables.sql: Creates the fruits table with vector column
# - 03_insert_data.sql: Populates the table with 16 sample fruits

# For example queries, see the ../examples/ directory
